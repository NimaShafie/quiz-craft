# data/

Runtime scratch directory for uploaded files (PDF/TXT) submitted through the QuizCraft UI.

- Files here are **not committed** (see `.gitignore`)
- This folder must exist for file upload to work — that is why `.gitkeep` is tracked
- Contents are temporary and can be cleared at any time
