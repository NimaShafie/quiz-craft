# QuizCraft — Python dependencies
# Tested with Python 3.11 / 3.12

# Web UI
streamlit>=1.40.0

# LLM backend (Ollama direct HTTP — no llama-index needed)
requests>=2.32.0

# PDF generation (quiz export)
fpdf==1.7.2

# PDF reading (file upload feature)
pypdf>=5.1.0

# Config parsing (stdlib, listed for clarity)
# configparser — stdlib

# NOTE: llama-index, torch, transformers etc. from the original requirements
# are NOT needed. They added ~4GB of install weight for features that are
# now handled directly via the Ollama HTTP API.
